name: TDS GA7 Release Gate

on:
  push:
    branches: [main]

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: 'TDS identity: 24f2000793@ds.study.iitm.ac.in'
        run: echo "Identity verified for TDS GA7"
